# okf-atlas

Dedicated Atlas store for the okf skill.

OKF root is `atlas/` (`atlas/SCHEMA.json`), not the git root.

```text
atlas mount github.com/sergio-sisternes-epam/okf-atlas --ref main
```

Then use root `.atlas/github.com/sergio-sisternes-epam/okf-atlas/atlas`.
